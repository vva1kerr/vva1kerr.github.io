The folder _layouts has to exist if building with jekyll (i think)

default.html is the page that displays the specified content. Thanks to jekyll it has a variable header linked to head-custom.html.

post.html no idea what it does