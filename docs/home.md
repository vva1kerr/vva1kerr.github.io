# Welcome

This is the home page. Drop any markdown file into `docs/`, add it to `manifest.json`, and it shows up in the nav.

## Features

- Zero build step
- Pure static files
- Just push to GitHub

```bash
git add docs/mynewpage.md
git commit -m "add new page"
git push
```
